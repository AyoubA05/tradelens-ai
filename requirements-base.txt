# Runtime dependencies shared by BOTH surfaces — the Streamlit app and the
# FastAPI backend. Neither file below duplicates these; both include this one.
#
# Versions are CI-verified on Python 3.11 (the deploy target) and additionally
# work on 3.9, which is what local development currently runs.
pandas==2.3.3
numpy==2.0.2
sqlalchemy==2.0.49
# PostgreSQL driver (Neon); SQLAlchemy uses it for postgresql:// URLs.
# 2.9.9, not 2.9.10: newest version shipping wheels for BOTH the local dev
# Mac (cp39 arm64) and the deploy target (cp311/cp312 manylinux).
psycopg2-binary==2.9.9
python-dotenv==1.2.1
anthropic==0.115.1
# Pillow 12.3.0 closes the July 2026 native-code/image-processing advisory
# set and is the production (Python 3.11) requirement. Pillow 12 dropped Python
# 3.9, which the current local Streamlit workflow still supports, so that
# interpreter remains on 11.3.0 until the local runtime moves. The API accepts
# only PNG/JPEG/WebP and does not invoke the affected font/PDF/JPEG2000/filter
# paths, but production must not carry a dependency scanners correctly flag.
pillow==11.3.0; python_version < "3.10"
pillow==12.3.0; python_version >= "3.10"
pydantic-settings==2.11.0
alembic==1.16.5
bcrypt==4.2.1
# IANA timezone database for zoneinfo — required on minimal containers that
# lack a system tz database (services/sessions.py uses ZoneInfo).
tzdata==2026.2
