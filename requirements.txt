# Runtime dependencies — installed on Streamlit Community Cloud (Python 3.11).
# Versions are CI-verified on 3.11. Dev/test/lint tools live in requirements-dev.txt.
streamlit==1.50.0
pandas==2.3.3
# Streamlit 1.50's AppTest dataframe conversion segfaults natively with
# PyArrow 25 on Python 3.11. Version 21 is verified by the Journal interaction
# flows and prevents the CI process from exiting with signal 11.
pyarrow==21.0.0
numpy==2.0.2
plotly==6.7.0
sqlalchemy==2.0.49
# PostgreSQL driver (Neon); SQLAlchemy uses it for postgresql:// URLs.
# 2.9.9, not 2.9.10: newest version shipping wheels for BOTH the local dev
# Mac (cp39 arm64) and the deploy target (cp311/cp312 manylinux).
psycopg2-binary==2.9.9
python-dotenv==1.2.1
anthropic==0.115.1
pillow==11.3.0
pydantic-settings==2.11.0
alembic==1.16.5
bcrypt==4.2.1
# IANA timezone database for zoneinfo — required on minimal Streamlit Cloud
# containers that lack a system tz database (services/sessions.py uses ZoneInfo).
tzdata==2026.2
