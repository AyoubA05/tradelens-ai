# Copy this file to .env and fill in your values

# Login credentials — the app is gated. Set these in production.
# If unset, the app falls back to demo / tradelens2025 so the public demo works.
# Once DB users exist (created via signup), the secrets login is no longer used.
TRADELENS_USERNAME=demo
TRADELENS_PASSWORD=tradelens2025

# Invite code that gates account signup. Leave unset to HIDE the signup form.
TRADELENS_INVITE_CODE=your-invite-code-here

# AI screenshot analysis (this app uses Anthropic)
ANTHROPIC_API_KEY=

# Database
DATABASE_URL=sqlite:///./data/tradelens.db

# AI model selection (override to swap models without touching code)
MODEL_PRIMARY=claude-fable-5
MODEL_GRADING=claude-haiku-4-5
MODEL_FALLBACK=claude-opus-4-8
EFFORT_DEFAULT=medium

# Anthropic client tuning
ANTHROPIC_TIMEOUT=120
ANTHROPIC_MAX_RETRIES=2

# DEMO_MODE=true returns cached/mock AI output — zero API spend (used in tests)
DEMO_MODE=false

ENV=development
